{
    "auto_react": {
        "description": "是否启用自动表情反应",
        "type": "bool",
        "default": false,
        "hint": "开启后，bot 会根据反应规则自动对收到的消息添加表情反应"
    },
    "react_rules": {
        "description": "表情反应规则",
        "type": "template_list",
        "templates": {
            "react_rule": {
                "name": "反应规则",
                "hint": "每条规则定义一组表情及其触发正则表达式，正则为空时对所有消息反应",
                "items": {
                    "emoji_ids": {
                        "description": "表情 ID 列表",
                        "type": "list",
                        "default": ["76"],
                        "hint": "QQ原生表情填数字(0-500)，如448=放大招；Unicode emoji直接填字符如😊"
                    },
                    "regex_list": {
                        "description": "触发正则表达式列表",
                        "type": "list",
                        "default": [],
                        "hint": "消息匹配列表中任意正则表达式时触发反应，留空则对所有消息反应"
                    }
                }
            }
        }
    },
    "react_scope": {
        "description": "自动反应范围",
        "type": "string",
        "default": "all",
        "options": ["all", "group", "private"],
        "hint": "all=所有消息, group=仅群聊, private=仅私聊"
    },
    "llm_react_enabled": {
        "description": "是否启用 LLM 表情反应函数工具",
        "type": "bool",
        "default": false,
        "hint": "开启后，LLM 可通过函数工具调用 react_to_current_message 和 react_to_message 来添加表情反应"
    },
    "enable_msg_id_prefix": {
        "description": "是否为消息注入消息 ID 前缀",
        "type": "bool",
        "default": true,
        "hint": "开启后，会在用户消息前添加 msg_id:xxx 前缀，以便 LLM 获取消息 ID 来调用 react_to_message 工具"
    }
}
